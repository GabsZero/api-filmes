{
  "version": 2,
  "builds": [
    {
      "command": "knex migrate:up && npm run build",
      "src": "src/index.ts",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.ts"
    }
  ]
}