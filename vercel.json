{
  "version": 2,
  "buildCommand": "knex migrate:up && npm run build",
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/index.ts"
    }
  ]
}